p.m.
* 复习小顶堆排序
* 学习堆、栈、队列的关系

**虽然有点抗拒，但这是一条必须经历的路**

* StringBuilder转String

`String str = "hello"; 
`StringBuilder sb = new StringBuilder(); 
`sb.append(str);

* 反过来：
`String str = sb.toString();

* StringBuilder清除内容
`StringBuilder str = "hello";`
`str.setLength(0);`

* queue的方法
|**offer(e)**|**添加元素**到队尾|失败返回 false|
|**poll()**|**删除并返回队首**|空返回 null|
|**peek()**|**查看队首**（不删）|空返回 null|
|**add(e)**|添加元素|失败抛异常|
|**remove()**|删除队首|空抛异常|